# freebuff-proxy 环境变量模板（可选，不建 .env 则全部用默认值）
# host 网络模式下服务直接监听宿主 0.0.0.0:<PORT>（不用写 docker 端口映射）
PORT=8787
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=127.0.0.1,localhost,172.16.0.0/12
TZ=Asia/Shanghai
